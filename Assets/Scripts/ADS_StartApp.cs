using UnityEngine;
using System.Collections;
using StartApp;

public class ADS_StartApp : MonoBehaviour {

	private int ibunner;
	private bool isShow;

	void Start () {
	//	isShow = false;
	//	#if UNITY_ANDROID
	//	StartAppWrapper.loadAd();
	//	#endif
	//	if (PlayerPrefs.HasKey ("bunner")) {
	//		ibunner = PlayerPrefs.GetInt ("bunner");		
	//	} else {
		//	ibunner = 0;	
		//}
	}
	
	void Update () {
		//if(ibunner == 0 && isShow == false) {
		//	if (Controller.die == true) {
		//		ShowBunner();
		//		isShow = true;
		//	} 
		//} 
	}

	//void ShowBunner() {
		//#if UNITY_ANDROID
		//StartAppWrapper.showAd();
		//StartAppWrapper.loadAd();
		//#endif
		//Debug.Log ("Bunner!!");
	//}

}
